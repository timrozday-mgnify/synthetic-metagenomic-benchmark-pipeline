nextflow_process {
    name "Test Process MAPSEQ_OTU"
    script "../main.nf"
    process "MAPSEQ_OTU"

    tag "modules"
    tag "mapseq"
    tag "mapseq/otu"

    test("majority-vote otu - stub") {
        tag "stub"
        options "-stub"
        when {
            process {
                """
                input[0] = [
                    [ id:'ampA' ],
                    file(params.test_data_base + 'genomeA.fasta', checkIfExists: true),
                    file(params.test_data_base + 'genomeB.fasta', checkIfExists: true)
                ]
                """
            }
        }
        then {
            assertAll(
                { assert process.success },
                { assert process.out.otu.size() == 1 },
                { assert snapshot(process.out.findAll { k, v -> k.startsWith("versions") }).match() }
            )
        }
    }
}
