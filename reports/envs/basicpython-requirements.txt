# basicpython execution-kernel env — everything the preprocessing script and the
# report's code cells import. Registered as the Jupyter kernel `basicpython`.
# pysam is the extra dependency vs a plain report env: it parses the reference BAMs.
ipykernel==6.29.5
ipython==9.0.0
pandas==2.2.3
numpy==2.2.6
plotly==6.7.0
pysam==0.24.0
PyYAML==6.0.2
# subspecies_infer.py: error-model mis-mapping (loads skiver .pt -> needs torch+pyro;
# context_error_models imports pyro, so pyro is mandatory anyway and numpyro would be
# redundant), analytic pairwise alignment (edlib), and Pyro NUTS/VI/MLE inference.
torch==2.6.0
pyro-ppl==1.9.1
edlib==1.3.9.post1
scipy==1.15.2
